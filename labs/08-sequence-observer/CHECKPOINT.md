# Checkpoint

Hint: line the two columns up fold by fold, not just their means. Means can differ by noise;
identical per-fold behaviour cannot.

1. What information does the sequence model have that your tabular observer did not?
2. What is the cheapest possible use of that same information?
3. Your two columns are close. What test tells you whether they are actually the same thing?
4. If the model ties the heuristic, what have you learned about the problem?
5. A variant reports ECE near zero and AUC near 0.5. What is it doing, and which phase-02
   idea does it belong to?
